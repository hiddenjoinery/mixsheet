# Capability specs

Each subdirectory captures the durable contract of one capability:
observable requirements (SHALL/MUST) with at least one testable scenario
each. Naming uses kebab-case (e.g. `mix-calculator`, `purchase-list`).

Specs are populated as features land via the OpenSpec change workflow —
see `../changes/`.
